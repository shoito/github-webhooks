name: CI Workflow for Frontend

on:
  workflow_dispatch:

jobs:
  run_ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run CI
        run: |
          echo "Running"

